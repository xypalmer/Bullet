<%= form_for(@user_vote) do |f| %>
  <% if @user_vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_vote.errors.count, "error") %> prohibited this user_vote from being saved:</h2>

      <ul>
      <% @user_vote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
